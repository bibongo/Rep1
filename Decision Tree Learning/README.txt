## Ravish Trivedi

The code is written in Python 3.3 language.

id3.py - uses information gain heuristic 

To run a script; use the following command
>> python <script name> <L> <K> <training set> <validation set> <test set> <to print?>
Example:
>> python id3.py 20 40 training_set.csv validation_set.csv test_set.csv yes

Make sure to give the to_print argument in lower case letters 'yes'.